# LAMBDA FUNCTION(used in django , frameworks etc)
# Create a lambda function to compute cube of a number
# when function has its use for one time only
# cant be used as a defined func eg cube()

cube = lambda x: x**3
print(cube(3))